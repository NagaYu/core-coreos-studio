# Core-CoreOS Studio — server dependencies (static host only).
# The AI runs in your browser; these just serve the page on localhost.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
